<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Propiedades -->
    <property name="LOG_FILE" value="logs/order-processing.log" />
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- Appender para consola -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Appender para archivo con rotación -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        
        <!-- Política de rotación: por tamaño y fecha -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Patrón de archivo rotado -->
            <fileNamePattern>logs/order-processing-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- Máximo tamaño de archivo antes de rotar -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Máximo días de retención -->
            <maxHistory>30</maxHistory>
            <!-- Máximo tamaño total de archivos -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Logger específico para órdenes -->
    <logger name="com.globalshop.order" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- Logger raíz -->
    <root level="WARN">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
